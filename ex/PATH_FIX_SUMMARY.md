# ✅ 路径问题修复总结

## 问题描述
运行`python ex/main.py latency --quick-test`时报错：
```
FileNotFoundError: 任务文件不存在: math_tasks_simple.json
```

## 根本原因
TaskLoader期望从`bft4agent-simple`目录加载任务（自动在`data/tasks/`下查找），但实验代码从其他目录运行。

## 解决方案

### 1. 修改工作目录切换逻辑
```python
# 修改前：切换到项目根目录
project_root = os.path.abspath(os.path.join(os.path.dirname(__file__), '../../..'))
os.chdir(project_root)

# 修改后：切换到bft4agent-simple目录
project_root = os.path.abspath(os.path.join(os.path.dirname(__file__), '../../..'))
bft4agent_dir = os.path.join(project_root, 'bft4agent-simple')
os.chdir(bft4agent_dir)
```

### 2. 配置文件中的路径
```yaml
# 正确的配置
tasks:
  file: "math_tasks_simple.json"  # TaskLoader会自动添加data/tasks/前缀
  num_tasks: 5
```

## ✅ 测试验证
```bash
$ python ex/main.py latency --quick-test

======================================================================
  BFT4Agent 实验系统
  实验类型: latency
======================================================================

快速测试模式
============================================================
实验配置: quick_test
总共 1 个实验配置

============================================================
配置 1/1
  节点数: 7
  恶意比例: 14.00%
  网络延迟: [10, 100]ms
  LLM后端: mock
============================================================

  任务 1/3: 1024 - 256 = ?
  ✓ 成功!
  端到端延迟: 1.61秒
  - PRE-PREPARE延迟: 0.506秒
  - PREPARE延迟: 0.106秒
  - COMMIT延迟: 0.082秒

结果: 3/3 成功
结果已保存: ex/results/data/experiment_20260128_143745.json
```

## 📝 修改的文件
1. `ex/experiments/latency/experiment.py` - 修复目录切换逻辑
2. `ex/configs/latency.yaml` - 修正任务文件路径

## 🎯 关键要点
- TaskLoader设计为从`bft4agent-simple`目录运行
- 配置中只需提供文件名，TaskLoader自动处理`data/tasks/`前缀
- 实验代码需要切换到正确的工作目录

**系统现在完全正常工作！** ✅
